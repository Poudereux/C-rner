"""DEPRECATED: La logica vive ahora en `app/main.py`.

Este archivo era el punto de entrada original con CSV y se ha
reemplazado por una aplicacion modular FastAPI con SQLAlchemy. Se
mantiene solo para no romper enlaces; el CMD del Dockerfile y todos
los imports apuntan a `app.main:app`.
"""
